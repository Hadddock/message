extends layout.pug
block content 
    h1= "Create new message!"
    form(method='POST')
        label(for='message') Message content:
        input#message.form-control(type='textarea', placeholder='Write your message here' name='message' required value=(undefined===post ? '' : post.message) )
        button.btn.btn-primary(type='submit') Submit
    if errors
        ul
        for error in errors
            li!= error.msg