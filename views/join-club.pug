extends layout.pug
block content 
    h1= "Join The Club!"
    h2= "Enter the password to join the club and view user information on posts"
    form(method='POST')
        label(for='password') Password:
        input#password.form-control(type='text', placeholder='Password' name='password' required value=(""))
        button.btn.btn-primary(type='submit') Submit
    if errors
        ul
        for error in errors
            li!= error.msg